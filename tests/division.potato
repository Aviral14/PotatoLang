int main() {
    int dividend; 
    int divisor;
    int quotient; 
    int remainder;
    dividend = 213;
    divisor = 3;

    /*Computes quotient*/
    quotient = dividend / divisor;

    /* Computes remainder */
    remainder = dividend % divisor;

    printf(quotient);
    printf(remainder);
}
