int main() {
    int dividend /* Error */
    int divisor;
    int quotient; 
    int remainder;
    dividend = 213;
    divisor = 3;

    /*Computes quotient*/
    quotient = [dividend / divisor];

    /* Computes remainder */
    remainder = (dividend % divisor);

    println(quotient);
    println(remainder);
}
